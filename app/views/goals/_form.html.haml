= notice
= simple_form_for @goal, :html => {:class => "form-horizontal"} do |f|
  .form-group
    = f.text_field :name, collection: goals_collection, as: :select,
      placeholder: 'click to display a list'
  %br
  .form-group
    = f.submit class: 'btn btn-default signin'
